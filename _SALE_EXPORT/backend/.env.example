# === TraderCopilot Backend .env.example ===
# Copia esto a backend/.env y ajusta valores.

# DB (Postgres recomendado en prod; SQLite en dev si no se define DATABASE_URL)
DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/tradercopilot
# Para SQLite dev podrías usar:
# DATABASE_URL=sqlite:///./tradercopilot.db

# JWT
SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 días

# LLM Providers (opcionales, pero PRO/Advisor los requerirá)
GEMINI_API_KEY=
DEEPSEEK_API_KEY=

# Telegram (opcional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_DEFAULT_CHAT_ID=

# CORS (si tu app lo soporta por env; si no, considera implementarlo)
CORS_ORIGINS=http://localhost:5173,http://localhost:4173,http://localhost:3000

# Scheduler / runtime
SCHEDULER_ENABLED=true
SCHEDULER_TICK_SECONDS=60
